# Airbnb Map Verification Status — 2026-08-24

The authenticated Airbnb host dashboard is reachable in the connected browser. The listings route was opened for read-only association and location review; its first rendered response was incomplete, so no listing location has yet been accepted as verified.

The listings table then loaded and confirms that a number of visible Horizon listing titles have a matching host-side listing. The table only presents city-level public location plus private host-management labels; it does not present a precise public map pin. Those labels are not copied into this repository, and they are insufficient by themselves to set exact latitude/longitude in Horizon.

One host listing was opened read-only and confirms a neighbourhood-level mismatch with its current Horizon record. The source supports correcting the public pin’s neighbourhood association, but its detailed private address is intentionally not stored in source control or surfaced in the public interface.

The corresponding city neighbourhood was independently located on a public map. Any Horizon correction will use that neighbourhood-level marker, rather than a building-level address.

A second matched host listing confirms that its Horizon record is associated with the wrong neighbourhood. Its public-map correction will likewise be limited to the verified neighbourhood centre, with building-level address data excluded from the repository and public product.

Two further matched listings confirm the same Al Yasmin neighbourhood association. They can share the established public neighbourhood marker instead of retaining their previously divergent, unverified coordinates.

Another matched listing confirms that its Horizon area label is too broad and that its current pin needs a neighbourhood-level review. Its detailed address remains excluded from source control and public map output.

Its general street area was confirmed on a public map. Any public Horizon pin will use this street-area marker rather than the private building location.

The direct editor route for one additional listing returned to the host list, although the host list confirms its matching title and a management-area cue. This is not enough to change its Horizon pin, so it remains pending a stable listing-detail view.

Two corrected Horizon property pages were opened live. Each now shows its verified neighbourhood label, a visible pin/tooltip, and a directions link driven by the updated general-area coordinate.

The public collection map is intentionally viewport-gated for page performance, so it displays its loading cue before the visitor reaches that section. Corrected pins were verified on their corresponding live property maps, where their labels, tooltip, and directions link are rendered.

A third corrected property page was opened live and shows the Al Malqa neighbourhood label, visible pin/tooltip, and working map-direction link.

An additional Hittin listing is confirmed from its read-only Airbnb detail. Its Horizon correction will use the public Hittin neighbourhood centre, not the property’s building address.

A matched penthouse listing confirms that its Horizon KAFD label should be replaced by the public As Sahafah neighbourhood marker. The building-level address is not retained or exposed.

A second matched listing also verifies the As Sahafah neighbourhood association. It can share the established general-area pin rather than the previous KAFD-labelled coordinate.

A matched garden suite listing confirms the Al Malqa neighbourhood association. Its Horizon pin will be aligned to a public neighbourhood marker rather than the unit’s detailed address.

A matched cinema suite listing confirms the existing Al Olaya neighbourhood association. Its map record remains unchanged pending no evidence of a coordinate error.

The one-bedroom television listing matches its Horizon unit profile and confirms an Al Yasmin location. Its current KAFD area label is therefore eligible for a neighbourhood-level correction only.

The Sky Lounge listing confirms an Al Qirawan location. Its prior Al Malqa association is not retained for the public map; any corrected pin will use the public neighbourhood marker only.

The studio listing matches the Horizon unit profile and confirms an Al Malqa location. Its previous Al Olaya pin can therefore be corrected to the established public Al Malqa marker.

The city-view suite listing confirms an Al Izdihar location. Its prior Al Olaya association is not retained for the public map; any corrected pin will use the public neighbourhood marker only.

One additional Horizon record points to a host listing whose title does not match the Horizon unit title. The source listing is in Al-Musayf, but the mismatched association is insufficient to modify the Horizon unit’s map pin. It remains pending a corrected channel association; the listing’s detailed address is excluded from all Horizon source and public presentation.

Another rooftop-oriented Horizon record also points to a non-matching Airbnb listing title. Its pin remains unchanged until the correct channel association is verified.

The designer-loft Horizon record points to an Airbnb listing with a different unit profile and bedroom count. Its map pin remains unchanged until the channel association is corrected.

The minimalist Horizon record points to a differently titled listing that exposes only city-level location. It does not provide sufficient evidence to update the unit’s map pin.

The royal-suite Horizon record points to an Airbnb listing with a different title and bedroom count. Its map pin remains unchanged until the channel association is corrected.

The pool-view Horizon record points to a differently titled Airbnb listing. Its map pin remains unchanged until the channel association is corrected.

The artistic-suite Horizon record also points to a differently titled listing with city-level location only. Its map pin remains unchanged until the channel association is corrected.

The direct Airbnb editor route for the massive three-bedroom record returned to the listings table. Its association and location remain pending a stable detail view.

The listing-table search confirms that the massive three-bedroom title exists in the host account, but the direct editor route remains unavailable for detailed location verification. No Horizon pin was changed.

The host-listing search confirms that the Tranquil Stay title matches the Horizon unit and provides a management-area cue for Al-Musayf. Its pin remains pending a stable detail view.

The matching detail view is now available and confirms the public Al-Musayf area association. Its Horizon correction will use the existing neighbourhood-centre marker only; the detailed building address is excluded.

A fourth corrected property page was opened live and now displays the Al Masif neighbourhood label, an active map pin/tooltip, and a directions link.

No Airbnb listing, calendar, price, media item, location, guest information, or host setting was changed. Horizon coordinate corrections remain blocked until each unit has specific, verified source evidence.
