# Extended Theme Options — Local Preview Notes

## Option 09 — Riyadh Market

The page loaded locally in Arabic as a full-page marketplace experience. The first viewport showed a full-screen hero, three or fewer actions, a fixed ambient background layer behind the page surface, and a verified property image as its active hero image.

After the data loaded, the hero counter advanced to `02 / 06`, confirming the shared 4-second property-image rotation was active. The page continued with a factual stay context panel, real property cards, a clear booking path, the interactive collection map, and a property-page booking entry.

## Option 15 — District Guide

The page loaded locally in Arabic as a distinct district-led theme. Its opening uses one primary action, its content explicitly keeps location information at neighbourhood level, and it retains the same verified-stay and map journey without copying the Option 09 marketplace structure.

## Option 18 — Horizon Portfolio

The full portfolio page loaded locally in Arabic after property data resolved. Its immersive hero, single direct action, real residence cards, booking path, and interactive map all render as a complete page. The visible hero counter had advanced to `05 / 06`, providing a second observed confirmation of the shared image rotation.
